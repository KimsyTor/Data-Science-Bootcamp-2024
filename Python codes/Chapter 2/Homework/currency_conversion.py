def currency_conversion(currency, amount):
    # your code
    # 1 create a function currency_conversion that take 2 parameters currency and amount
    # 2 currency_conversion return 'invalid amount' if the amount parameter is not integer or float.
    # 3 currency_conversion return 'not found' if the currency parameter is not usd, yuan or bath.
    # 4 if everything correctly provided, this function should return conversion (rate * amount) amount as result
    ...
